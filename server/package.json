{
  "name": "henrycraft-sync",
  "private": true,
  "version": "1.0.0",
  "description": "Multiplayer sync for Henrycraft: one Durable Object per district.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
